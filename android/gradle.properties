org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Daemon JVM is selected via android/gradle/gradle-daemon-jvm.properties
# (toolchainVersion=21). org.gradle.java.home is intentionally omitted here
# because Gradle reads it from ~/.gradle/gradle.properties only — setting it
# in a project's gradle.properties has no effect on the launcher JVM.
